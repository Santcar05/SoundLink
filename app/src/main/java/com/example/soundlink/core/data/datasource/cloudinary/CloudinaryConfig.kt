package com.example.soundlink.core.data.datasource.cloudinary
/*
import android.content.Context
import com.cloudinary.android.MediaManager

// unsigned Uploads configuration
object CloudinaryConfig {
    private const val CLOUD_NAME = "dkidrkkjk"
    private const val UPLOAD_PRESET = "your_upload_preset"


    fun initiliaze(context: Context){
        // Cloud_name is only used to initialize the SDK
        val config = mapOf(
            "cloud_name" to CLOUD_NAME
        )

        MediaManager.init(context, config)
    }


    fun getCloudName() = CLOUD_NAME

    fun getUploadPreset() = UPLOAD_PRESET
}

 */